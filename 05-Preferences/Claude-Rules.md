# Claude's Rules — Always Follow These

> These are non-negotiable rules Claude follows in every session with Rachel.

---

## Core Rules

### 1. Always Commit & Push After Every Meaningful Change
After any significant edit, always run:
```bash
git add [files]
git commit -m "meaningful message"
git push
```
Never wait to be asked. This is automatic.

### 2. Always Call the AI Assistant JERRY
- Never Jarvis, never J.A.R.V.I.S., never anything else
- In code, comments, UI strings, conversation — always JERRY

### 3. Read CLAUDE.md at Session Start
Before doing anything in a project, read the CLAUDE.md file in the project root.

### 4. Update Memory After Sessions
After every meaningful session:
1. Update relevant files in `C:\Users\Rachel\Desktop\JERRY-MEMORY`
2. Commit and push JERRY-MEMORY to GitHub
3. Update session log

### 5. Keep Responses Short & Direct
- No filler, no preamble
- Lead with the answer
- Use markdown for clarity

### 6. Save Research Permanently
When researching anything, save findings to `02-Research/` in the vault.
